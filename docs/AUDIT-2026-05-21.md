# Debrief Audit — 2026-05-21

Audit run at v1.9.8 on `main`. Health + security + functional QA via static review and dependency audits. App was not run interactively (Electron desktop, no automated harness).

**Original verdict (at audit time):** Not ready for broader release. Recent fix sprint was sound, but audit found 3 critical security holes, 3 critical functional bugs (incl. silent data loss), and a broken dev environment.

**Status update — 2026-05-21 (post-Tier-1):** All of **Tier 0 and Tier 1 are fixed** and on `main`. `npx tsc --noEmit` is **0 errors**, `npm run build` compiles **clean (0 warnings)**, `npm audit` is **0 vulnerabilities**, and the suite is **green (157 passing)**. Phase E's runtime-dependent changes (Electron 42 bump, sandbox, sidecar auth, ffmpeg/safe-file, LanceDB fixes) still need a launch + `npm run pack` on macOS/Windows/Linux to confirm — see the per-item notes. Remaining: Tier 2 hygiene (8 items below; several already done in passing).

**Composite health score:** was 1.8 / 10 at audit time (typecheck partial, tests blocked). Tier 0 + green build/tests lifts the baseline materially; Tier 1 (sidecar auth, exec→execFile, sandbox, dep advisories) is the next lever.

This file is the tracking checklist. Tick boxes as fixes land. Add a commit SHA next to each box so we can trace what shipped where.

---

## Tier 0 — block ship

Each item is small and surgical. Target: one branch (`tier0-critical-fixes`), one commit per fix.

- [x] **C-FN-2: Enable SQLite foreign keys.** `public/electron.js:197-207` — add `db.pragma('foreign_keys = ON')` after `new Database()`. Schema declares `ON DELETE CASCADE` on `project_transcripts`, `project_chat_conversations`, `project_chat_messages`, `project_analysis`, `transcript_segments`, `transcript_topics` — all currently no-ops. Add a regression test that deletes a project and asserts orphan rows are gone. _(branch `tier0-critical-fixes`, regression suite at `tests/database-cascades.test.ts`)_
- [x] **C-FN-1: Fix `change-database-location` filename mismatch.** `public/electron.js:964` writes `locallisten.db`; `initDatabase` (line 197, 207) opens `audio-scribe.db`. After "move," next launch opens empty DB in new folder — library appears to vanish. Either rename to `audio-scribe.db` or change `initDatabase` to use a single source of truth (preferred: a `DB_FILENAME` const). While here, decide whether sidecar/venv/vectordb move too (see H-15). _(Used `DB_FILENAME` const; H-15 deferred to Tier 1.)_
- [x] **C-FN-3: Crash-proof `JSON.parse` on transcript columns.** `src/contexts/TranscriptContext.tsx:49-63` (and `getTranscript`, `searchTranscripts`) — wrap each of the 9 JSON columns in try/catch with sensible defaults. Same in `public/electron/vector-store.js:186, 230, 268` for the `speakers` column. One corrupt row currently empties the library silently. _(Deduped the three identical hydration blocks into `hydrateTranscriptRow`; added `parseJsonOr`/`parseSpeakers` helpers that log+default per field.)_
- [x] **C-SEC-2: Fix stored XSS in transcript search highlighter.** `src/components/ProjectCrossTranscriptSearch.tsx:49-54, 422, 450` — `highlightText` interpolates raw text into HTML, fed to `dangerouslySetInnerHTML`. Replace with a function returning React children (`<mark>` elements around matched substrings). HTML-escape first if you must keep the string-based shape. _(Returned `React.ReactNode`, removed both `dangerouslySetInnerHTML`. Regression suite at `tests/ProjectCrossTranscriptSearch.xss.test.tsx` — including a malicious `<img onerror>` payload that asserts no img element materialises.)_
- [x] **C-SEC-1: Scope down `fs-*` IPCs.** `public/electron.js:1487-1526` — `fs-read-file`, `fs-write-file`, `fs-delete-file` accept any path. Restrict to paths under `app.getPath('userData')` (resolve + canonicalize + check prefix) or paths the user just selected via a dialog (track recent picks). Reject symlinks. Reject calls from non-top-frame senders. _(Discovered `fs-read-file` and `fs-write-file` had zero renderer callers — deleted them entirely (preload, IPC, types). `fs-delete-file` scoped to `os.tmpdir()` via shared `assertPathUnderTmp` in `public/electron/safe-paths.js`; rejects `..`-traversal, prefix-match-but-not-child, and symlinks pointing outside. Regression suite at `tests/safe-paths.test.ts`.)_
- [x] **C-SEC-3 + H-1: Kill the generic `db-query` IPC.** ✅ DONE. Phased migration; the renderer can no longer run arbitrary SQL — every DB access goes through a validated per-domain RPC under `electronAPI.db.*`, and the column-injection hole in `transcripts`/`projects` `update()` (H-1) is closed by an allow-list. See `docs/TIER-0.6-HANDOVER.md` for the full record.
  - [x] **Phase 1 — infrastructure + `settings` domain** (commit `340597a`). 27 raw-SQL sites replaced with `electronAPI.db.settings.*`. Tests at `tests/db-rpc-settings.test.ts`.
  - [x] **Phase 2 — `transcripts` + `projects` domains** (merged `21e03cf`). 36 single-table sites; `update()` allow-lists close H-1.
  - [x] **Phase 3 — the 8 small domains** (`projectTranscripts`, `chat`, `projectChat`, `transcriptSegments`, `topics`, `projectAnalysis`, `modelMetadata`). 44 remaining sites migrated; one test file per domain.
  - [x] **Phase 4 — deleted the generic `db-query` IPC + `database:` preload key/type + added a guard.** Guard is both a vitest test (`tests/no-generic-db-query.test.ts`, runs every suite) and a CI grep step in `release.yml`'s security-audit job; both fail if `electronAPI.database` or `ipcMain.handle('db-query'` reappears. `grep` confirms zero live callers. Suite: 148 passing; tsc unchanged (18 pre-existing Tier 2 errors).

Once Tier 0 is merged, the app is shippable to friendly users (still has HIGH issues but no data-loss or RCE-equivalent paths). **Note: C-SEC-3 is the only Tier 0 item that requires multiple branches to fully close.**

---

## Tier 1 — next sprint

Roughly in priority order. Branch suggestion: `tier1-high-fixes`.

- [x] **H-3: Sidecar shared-secret auth.** _(Phase B.)_ `SidecarManager` generates a per-launch `crypto.randomBytes(32)` token, passes it via `DEBRIEF_SIDECAR_TOKEN` env to `server.py`, which enforces `Authorization: Bearer <token>` (constant-time compare) in an HTTP middleware on every route; the manager's `/healthz` poll and all `sidecar-client.js` calls (`/analyse`, `/rediarise`, `/embed`) send the header. Fails open with a warning only if the env var is unset (manual dev runs). **Needs an app launch to confirm the sidecar still starts + serves.**
- [x] **H-4: Replace `exec` with `execFile`/`spawn` for ffmpeg.** _(Phase B.)_ All four shell-string ffmpeg calls in `electron.js` (transcode, duration probe ×2, extract-audio) now use `execFile` with an argv array — no shell, so paths can't inject. Sidecar already used `spawn`. **Needs an app launch to confirm ffmpeg extraction/probe still work.**
- [x] **H-5: Lock down `safe-file://` protocol.** _(Phase B.)_ The handler now allow-lists requested paths against `transcripts.file_path` (both sides `path.resolve`-normalised) before streaming, so XSS can no longer `fetch('safe-file:///…/.ssh/id_rsa')`. **Needs an app launch to confirm media playback still works.**
- [x] **H-2: Sanitize LanceDB filter strings.** _(Phase B; superseded by H-18.)_ Originally added `sqlStringLiteral()` escaping at the 4 `.where()`/`.delete()` interpolations. H-18 then removed all string `.where()`/predicate usage entirely (the LanceDB 0.20 bug made them no-ops anyway), so the injection surface is gone and the escaping helper was deleted — filtering/deleting now happens in JS / via rebuild, with no renderer-controlled string reaching a Lance SQL predicate.
- [x] **H-6: Enable Electron sandbox.** _(Phase E.)_ Set `sandbox: true` in `createWindow` webPreferences and dropped `--no-sandbox` from the `electron`/`electron-dev` scripts. The preload is sandbox-safe (contextBridge/ipcRenderer/webUtils + process.platform/versions only; all privileged ops go over IPC). **Verify by launching on macOS, Windows, and Linux. Linux note: if dev launch hits "SUID sandbox helper ... not configured correctly", fix node_modules/electron/chrome-sandbox perms (root:root 4755) — do NOT re-add --no-sandbox to the packaged path.**
- [x] **H-7: Verify python-build-standalone SHA-256.** _(Phase C.)_ `bundle-python.sh` now downloads the release's aggregate `SHA256SUMS`, extracts the exact asset's digest (awk `$2 ==`, so the `_stripped` variant isn't confused), and fails closed on mismatch/missing before extracting. (PBS publishes one `SHA256SUMS`, not per-asset `.sha256` sidecars — verified.)
- [x] **H-8: Pin HuggingFace model revisions.** _(Phase C.)_ All 6 repos in `prefetch-models.py` now carry a `revision=<commit-sha>` passed to `snapshot_download`; the loop hard-errors if any entry lacks one. SHAs resolved via the HF metadata API (works for gated repos too).
- [x] **H-9: Pin GHA actions to commit SHAs.** _(Phase C.)_ All `uses: actions/*` in `release.yml` + `ci.yml` pinned to commit SHAs with `# vN` comments; added `.github/dependabot.yml` (github-actions + npm, grouped minor/patch) so the pins stay current.
- [x] **H-10: Bump Electron** (≥ v42). _(Phase E.)_ Bumped to `^42.2.0` (clears GHSA-5rqw-r77c-jp79 + GHSA-xj5x-m3f3-5x3h); `npm audit` now 0 vulns. Only stable APIs in use, no source changes. **Needs launch + native-rebuild verification on mac/win/linux (see commit `9a85216`).**
- [x] **H-11: Resolve `electron-rebuild` advisories.** _(Phase E.)_ The standalone `electron-rebuild` was unused (electron-builder's bundled `@electron/rebuild@4` does the rebuilds), so it was removed entirely — cleared 6 high + 2 low advisories.
- [x] **H-12: Windows filename parsing.** _(Phase A.)_ The renderer has no Node `path` module, so added a cross-platform `getBasename` helper (splits on `/` and `\`) in `src/utils/helpers.ts` and used it at all 5 sites (UploadPage ×3, ProcessingQueue, TranscriptDetailPage).
- [x] **H-13: Cleanup extracted WAVs on error/cancel.** _(Phase A.)_ Hoisted `audioPath` above the try and moved `deleteFile` into a `finally` in `fileProcessor.ts`, so cancelled/failed video imports no longer leak temp WAVs.
- [x] **H-14: Auto-update — wired up.** _(Phase D; chose to make it real.)_ Installed `electron-updater@6.8.3` (runtime dep) and added `setupAutoUpdater()` in `electron.js`: packaged-only `checkForUpdatesAndNotify()` with event logging. Feed comes from the existing `electron-builder.json` `publish` (github/michael-borck/debrief) via app-update.yml. **Needs a real tagged release to verify end-to-end; macOS auto-update requires the signed+notarized zip target (present). Windows builds are unsigned, so Win auto-update will warn — revisit when Win signing lands.**
- [x] **H-15: `change-database-location` half-truth.** _(Phase D; chose clarify.)_ Settings now states "Change Location moves the database file only" (models/venv/vector store stay in app data), and the success alert says "Database file moved."
- [x] **H-16: `isMounted` / cancellation in async `useEffect`s.** _(Phase E.)_ Added an `isMountedRef` guard (existing ServiceContext pattern) to the async loaders in TranscriptDetailPage, ProjectDetailPage, ProjectChatModal, and TranscriptChatModal so a load resolving after navigate/close can't overwrite newer state. TopicsTab already had it; app-lifetime context providers excluded (don't unmount).
- [x] **H-17: Trash "30-day auto-delete" — now real.** _(Phase D; chose implement.)_ Added `db-rpc/maintenance.js` `sweepExpiredTrash()` (purges transcripts/projects with `deleted_at` older than 30 days; FK cascade clears children), called once per launch in `electron.js` after `initDatabase`. Tested in `tests/db-rpc-maintenance.test.ts` (3 cases incl. cascade).
- [x] **H-18: LanceDB `.where()` bug IS broader — verified + fixed.** _(Phase E.)_ Confirmed against real LanceDB 0.20: string-equality predicates on Utf8 columns return 0 rows (numeric work, strings don't — regardless of quoting). So `searchSimilar` (with transcriptId/speaker) returned nothing — **transcript-scoped chat RAG was silently broken** — and every predicate delete (`deleteTranscriptChunks`, `updateChunks`) was a no-op, leaking orphaned vectors. Fixed: searchSimilar over-fetches + filters in JS; deletes read/filter/rebuild the table. Added `tests/vector-store.test.ts` (6 cases) exercising the real module end-to-end. Also guarded `require('electron')` so the module is unit-testable. Real long-term fix: upgrade LanceDB past the Utf8-predicate bug, then revert to `.where()`.

---

## Tier 2 — hygiene

Branch suggestion: `tier2-hygiene`.

- [x] **Repair dev environment.** _(Done.)_ Root cause was a corrupted install (react-router type entry + docx runtime files missing), not just stale deps; reinstalled from the lockfile. `npx vitest run` (148) and `npm run build` (clean) both pass.
- [x] **Add CI sanity gate.** _(Phase A.)_ `.github/workflows/ci.yml` runs `npm ci --ignore-scripts` + `tsc --noEmit` + `npm run build` + `vitest run` on every push.
- [x] **Add a linter.** _(Tier 2.)_ Added Biome (`biome.json`) + `lint`/`lint:fix`/`format` scripts. Config is tuned to high-signal rules (a11y, noExplicitAny, useExhaustiveDependencies, useImportType, etc. disabled — the latter because tsconfig uses the classic `jsx: "react"` runtime, so `import type React` breaks JSX). Deliberately NOT bulk-auto-fixed: Biome's "safe" fixes broke the build/tests here, so cleanup is left to run incrementally via `npm run lint`. (CI gate is unchanged — lint is advisory, not blocking.)
- [x] **Fix `react-router-dom` v7 imports.** _(Done.)_ Root cause was `moduleResolution: node` (node10) can't read v7's `exports`-map subpath re-export; switched tsconfig to `moduleResolution: bundler` + `module: esnext`. 0 errors.
- [x] **Fix `docx` module resolution.** _(Done.)_ docx's `dist/index.*` files were missing from the corrupted install; restored by reinstall.
- [x] **Regression tests for the last 5 bug fixes.** _(Tier 2.)_ `fabcbc1` + `7cd48a7` (getTranscriptChunks LanceDB workaround) are covered behaviourally by `tests/vector-store.test.ts`. `64b60fd` (bundle *.py), `6955aed` (auto-update manifests / publish config), and `238c936` (npm/pip audit release gate) are guarded by `tests/regression-config.test.ts`. The remaining code bits of `6955aed` (fileProcessor error-row deletion, ffmpeg app.asar.unpacked path) are integration-level — flagged for the launch test.
- [x] **Tests for the untested critical paths.** _(Tier 2 — the unit-testable ones.)_ `vector-store.js` (`tests/vector-store.test.ts`, 6), `processingScheduler.runSerial` (`tests/processingScheduler.test.ts`, 5 — serial-mutex invariants), `exportService` DOCX/PDF builders (`tests/exportService.test.ts`, 4 smoke), and `topicsService` clustering helpers (`tests/topicsService.test.ts`, 4 — exported squaredDistance/mean/kmeans). `fileProcessor.ts`, `chatService.ts`, and the sidecar lifecycle are IPC/process-bound (heavy mocking) — left for integration coverage, exercised by the Phase E launch test.
- [x] **Delete orphan `vector-store.js` at repo root** _(Phase A.)_ — already absent (removed in an earlier session); nothing to do.
- [x] **Delete dead `src/services/audioExtractor.ts`** _(Phase A.)_ — confirmed zero callers, deleted.
- [x] **Gate `console.log/.warn/.error` in `src/components/`** behind a dev-only logger. _(Tier 2.)_ Added `src/utils/logger.ts` (log/warn/info/debug dev-only via webpack NODE_ENV, error always-on) and swapped all 49 `console.*` calls across 18 components to `logger.*`.
- [ ] **CSP: drop `'unsafe-inline'`.** `public/index.html:6`. Move the `window.global = window` shim into the bundle. Narrow `connect-src` from `https:` to actual AI provider hosts.
- [ ] **Don't return cleartext API keys to renderer.** `crypto-decrypt-string` IPC + `chat-with-ollama` flow (`public/electron.js:1080-1118`). Have main make outbound calls instead of handing keys back.
- [x] **Wrap `topicsService.ts:319-342` topic-save loop in `db.transaction()`.** _(Done in Tier 0.6.)_ The save moved into the `topics` RPC as an atomic `replaceForTranscript` (DELETE + inserts in one `db.transaction`).
- [x] **Surface `safeStorage` decrypt-empty.** _(Tier 2.)_ The chat handler now detects "a key was stored but decrypted to empty" and returns a clear "re-enter your API key in Settings" error — but only for providers that require a key (keyless Ollama with a stale key is unaffected). `decryptIfNeeded` also logs both failure branches.
- [x] **Fix `getMediaInfo` duration parsing** to include centiseconds (cosmetic). _(Tier 2.)_ Regex now captures the fractional part (`00:01:23.45` -> 83.45s).

---

## What this audit did NOT cover

- Interactive QA of the running Electron app (no browser-QA harness fit; code review substituted)
- Python `pip-audit` against the sidecar venv (`pip-audit` not installed locally; CI runs it on release)
- End-to-end LanceDB behavior verification (would need a live multi-transcript DB)
- Auto-update flow runtime testing (would need a tagged release)
- Sidecar shutdown race on macOS quit-from-dock vs Cmd-Q vs window-close
- Drag-and-drop path resolution (`webUtils.getPathForFile` requires real drag events)
